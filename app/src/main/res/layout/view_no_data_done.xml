<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/titleEmpty"
        style="@style/HeadingSubTitle"
        android:layout_marginTop="128dp"
        android:text="@string/no_data_done"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/contentEmpty"
        style="@style/BodySubContent"
        android:layout_width="0dp"
        android:layout_marginTop="8dp"
        android:layout_marginStart="-24dp"
        android:layout_marginEnd="-24dp"
        android:text="@string/no_data_done_message"
        android:textAlignment="center"
        android:textColor="?attr/colorControlActivated"
        app:layout_constraintEnd_toEndOf="@+id/titleEmpty"
        app:layout_constraintStart_toStartOf="@+id/titleEmpty"
        app:layout_constraintTop_toBottomOf="@+id/titleEmpty" />

</androidx.constraintlayout.widget.ConstraintLayout>